
import React from 'react';

// This component is deprecated and replaced by FireLayer.tsx
// Keeping a null render to prevent import errors if lazily loaded, 
// though standard practice is to remove usages.
const Cursor: React.FC = () => {
  return null;
};

export default Cursor;
